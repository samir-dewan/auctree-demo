import { Text, View, StyleSheet } from "react-native";


function PropertySlider({propertyDetails}) {
    // for the front page, will have a forward and back arrow to click through each property.
    return (
        <View>
                <Text>PropertySlider</Text>
        </View> 
    )
}

const styles = StyleSheet.create({

})

export default PropertySlider;