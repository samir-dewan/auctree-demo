class Review {
    constructor(
        id,
        user_id,
        username,
        place_id,
        rating,
        review
    )
    {
        this.id = id,
        this.user_id = user_id,
        this.username = username,
        this.place_id = place_id,
        this.rating = rating,
        this.review = review
    }
};

export default Place;